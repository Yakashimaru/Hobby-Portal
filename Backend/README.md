# Setup
## Generating requirements.txt
1) CD to backend
2) Enter `pipreqs .` into the cli

## Importing and exporting postgressql
### Importing
1) Using CLI: `
pg_dump -U dbusername dbname > dbexport.pgsql
`
### Exporting
